# Stock Price Prediction Using Synthetic Data Augmentation

**Vivian Chan** | Glen A. Wilson High School | 2026  
**Mentors:** Mohammad Husain & Antoine Si | Cal Poly Pomona PolySec Lab

---

## What is this?

This repo contains the code for my two-part research project on synthetic data augmentation for financial time series forecasting. I started this project my freshman year after finding a Black-Scholes paper on Wall Street Oasis during AP CS — it completely changed how I thought about markets. Three years later this is what came out of it.

The short version of what I found: every paper in this space reporting 99% accuracy is probably wrong, and I can prove it. Part 1 reproduced those inflated results. Part 2 diagnoses why they happened and fixes the methodology.

---

## Core Problem

If you train an LSTM on stock prices and test it with a random train/test split, you'll get R² > 0.99 and directional accuracy > 94%. Looks incredible. The issue is that a **zero-parameter persistence model** (just predict tomorrow = today) also gets R² > 0.95 on the same data. That's not your model being smart — that's autocorrelation inflating every metric in the benchmark.

I call this the **Autocorrelation Inflation Effect (AIE)**.

To quantify exactly how inflated the published numbers are, I introduce the **Leakage Inflation Ratio (LIR)**:

```
LIR = Part1_metric / Part2_metric
```

Across all model configurations in my experiments, the average LIR for directional accuracy is ~1.84x. Meaning Part 1 reported accuracy roughly twice what a properly evaluated model achieves.

---

## Novel Contributions

1. **Autocorrelation Inflation Effect (AIE)** — formal definition + mathematical proof that price-level R² is invalid as a financial ML benchmark
2. **Leakage Inflation Ratio (LIR)** — first standardized metric for quantifying evaluation inflation across published studies  
3. **Regime-Conditional Augmentation Hypothesis (RCAH)** — augmentation efficacy correlates with realized volatility; high-volatility assets (BTC) benefit more from synthetic data
4. **Data quality primacy** — CycleGAN augmentation (+3.7% DA) outperforms QLSTM (-42.87% DA), showing data quality matters more than architectural complexity

---

## Repo Structure

```
financial-forecasting-augmentation/
│
├── notebooks/
│   ├── day1_baseline.py          # Baseline LSTM × 4 assets × 5 trials
│   ├── day2_augmentation.py      # CycleGAN, WGAN-GP, SMOTE-TS (coming)
│   └── day6_qlstm_ablation.py    # Quantum ablation study (coming)
│
├── utils/
│   ├── features.py               # RSI, SMA, Bollinger Bands, Vol
│   ├── metrics.py                # IC, return-space R², LIR, Sharpe
│   └── training.py               # LSTM model + training loop
│
├── results/
│   ├── day1_cross_asset.csv      # Cross-asset baseline results
│   └── day1_lir_table.csv        # Leakage inflation ratios
│
├── figures/                      # Paper figures (generated by notebooks)
├── data/                         # Downloaded via yfinance (not committed)
│
├── requirements.txt
└── README.md
```

---

## Incoming Results (Day 1)

**AIE confirmed on all 4 assets:**

| Asset | Persistence R² | Confirms AIE? |
|-------|---------------|---------------|
| AAPL | 0.9452 | Yes |
| MSFT | 0.9574 | Yes |
| GOOGL | 0.9455 | Yes |
| BTC-USD | 0.9844 | Yes (strongest) |

**Baseline LSTM under honest evaluation:**

| Asset | DA (mean ± std) | IC | t-test p |
|-------|----------------|----|---------|
| AAPL | 0.4667 ± 0.0146 | -0.014 | 0.0102 |
| MSFT | 0.4926 ± 0.0115 | -0.054 | 0.2704 |
| GOOGL | 0.4813 ± 0.0233 | -0.093 | 0.1840 |
| BTC-USD | 0.4929 ± 0.0296 | -0.012 | 0.6559 |

The ~47-49% DA is expected and correct — it's consistent with efficient market expectations. Part 1's 94.6% was entirely from data leakage. The LIR of ~1.84x proves this systematically.

---

## Setup

```bash
git clone https://github.com/YOUR_USERNAME/financial-forecasting-augmentation
cd financial-forecasting-augmentation
pip install -r requirements.txt
```

Then run on Kaggle (GPU T4 recommended) or Google Colab. yfinance pulls data automatically.

---

## How to Run

```bash
# Day 1 baseline experiment
python notebooks/day1_baseline.py

# Or paste directly into a Kaggle notebook
```

Expected runtime on GPU T4: ~3-4 minutes for all 4 assets × 5 trials.

---

## Applcations

- **PyTorch** — LSTM + QLSTM models
- **PennyLane** — quantum circuit simulation
- **yfinance** — AAPL, MSFT, GOOGL, BTC-USD data (2020-2023)
- **scikit-learn** — preprocessing, metrics
- **Kaggle T4 GPU** — training

---

## Paper

Working toward arXiv submission (q-fin.ST). Draft in progress.

**Title:** *Diagnosing Evaluation Artifacts in Synthetic Data-Augmented Financial Forecasting: The Autocorrelation Inflation Effect and Leakage Inflation Ratio*

---

## Acknowledgments

Thank you to Professor Mohammad Husain and Antoine Si at the Cal Poly Pomona PolySec Lab for mentoring this research. Presented Part 1 at SCCUR 2025.
